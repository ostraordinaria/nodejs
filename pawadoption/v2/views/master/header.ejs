<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" href="../../public/images/dog-icon.ico" type="image/ico" sizes="16x16">
    <link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" type="text/css" href="/stylesheet/custom.css">
    <style>
    .card-columns {
        -webkit-column-count: 4;
        column-count: 4;
    }
    </style>
    <title>Paw Adoption</title>
</head>

<body>
    <nav class="navbar fixed-top navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="/">Paw Adoption</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                </ul>
                <form class="form-inline">
                    <ul class="navbar-nav mr-auto">
                        <%if(!currentUser){%>
                            <li class="nav-item">
                                <a class="nav-link" href="/register">Sign Up <i class="fa fa-user-plus"></i></a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/login">Login <i class="fa fa-user"></i></a>
                            </li>
                            <%} else { %>
                                <li class="nav-item">
                                    <a class="nav-link">Signed in as <%= currentUser.username%> <i class="fa fa-user-circle-o"></i></a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="/logout">Logout </a>
                                </li>
                                <% }%>
                    </ul>
                </form>
            </div>
        </div>
    </nav>
    <% if(flash.error) {%>
        <div class="container">
            <div class="alert alert-danger" role="alert">
                <%= flash.error%>
            </div>
        </div>
        <%}%>
            <% if(flash.welcome) {%>
                <div class="container">
                    <div class="alert alert-success" role="alert">
                        <%= flash.welcome%>
                    </div>
                    <%}%>
                </div>
                <% if(flash.logout) {%>
                    <div class="container">
                        <div class="alert alert-success" role="alert">
                            <%= flash.logout%>
                        </div>
                        <%}%>
                    </div>